# _

This top-level directory holds inert historical experiments, code sketches, and
design records that are worth preserving but are not part of ICU's active build.

Nothing under `_/` should be imported, compiled, packaged, or treated as a
current implementation merely because it is present. Its purpose is to keep
expensive exploratory work available for later reconstruction without forcing
obsolete architecture into the live tree.
